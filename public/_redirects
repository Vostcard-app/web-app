# API routes should go to Netlify functions (MUST come before catch-all)
/api/*   /.netlify/functions/:splat   200

# Functions are handled automatically by Netlify

# Handle client-side routing (catch-all MUST be last)
/*    /index.html   200 