import React from 'react';
import PublicVostcardView from './PublicVostcardView';

const SharedVostcardView: React.FC = () => {
  // For now, just render the existing public view
  // We'll enhance this in a future update
  return <PublicVostcardView />;
};

export default SharedVostcardView; 